@model IEnumerable<Review.Models.Itemreview>

@{
    ViewBag.Title = "The actual Reviews";
}

<h2>REVIEWS</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Item.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WrittenReview)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WrittenReview)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ItemreviewID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ItemreviewID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ItemreviewID })
        </td>
    </tr>
}

</table>
